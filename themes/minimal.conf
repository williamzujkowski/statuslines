# minimal — the four things you actually look at.
name        = minimal
description = Directory, branch, context, cost
requires    = none

line1 = dir git context cost

separator       = " · "
separator_color = dim

dir_style     = basename
context_label = ctx
width_reserve = 6
drop_order    = cost context git dir
